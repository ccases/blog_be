%h1= @article.title

%p= @article.content
= link_to "Back to index", articles_path
%hr

%table
  %thead
    %tr
      %th Comment
      %th Actions
  %tbody
    - @comments.each do |comment|
      %tr
        %td= comment.content

= render 'comments/form'